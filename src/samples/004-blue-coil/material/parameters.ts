export type CoilParameters = { coilSpacing: number; coilThickness: number; centerLift: number; radialDrift: number; bindingFrequency: number; indigoDensity: number; compression: number; lightDirection: number };
export const DEFAULT_COIL_PARAMETERS: CoilParameters = { coilSpacing:.54, coilThickness:.62, centerLift:.72, radialDrift:.46, bindingFrequency:.56, indigoDensity:.42, compression:.68, lightDirection:.32 };

